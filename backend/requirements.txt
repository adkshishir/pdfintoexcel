# --- web ---
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20

# --- config ---
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0
PyJWT==2.10.1

# --- db ---
sqlalchemy==2.0.36
psycopg[binary]==3.2.3
alembic==1.14.0

# --- queue ---
celery[redis]==5.4.0
redis==5.2.1

# --- pdf / tables (phases 2–6) ---
pymupdf==1.25.1
pdfplumber==0.11.4
# Plain camelot-py (no [cv]): the cv extra pulls pdftopng>=0.2.3, which has no
# manylinux aarch64 wheels on PyPI, so `docker build` fails on ARM64. Lattice
# mode may need extra system deps later; stream / fallbacks use the base stack.
camelot-py==0.11.0

# --- ocr (phase 3) ---
# paddleocr pulls paddlepaddle (~300MB wheel). Models (~1.5GB) download to
# the `paddle-models` volume on first call. Tesseract is the auto-fallback.
paddleocr==2.9.1
paddlepaddle==2.6.2
pytesseract==0.3.13
pillow==12.3.0

# --- excel (phase 5) ---
pandas==2.2.3
openpyxl==3.1.5
# numpy capped at 1.x because paddleocr 2.9.1 requires numpy<2.0 (camelot is
# happy with anything >=1.13.3, pandas 2.2 supports both 1.x and 2.x).
# When PaddleOCR ships a 2.x-compatible release this pin can move up.
numpy==1.26.4

# --- storage (phase 8) ---
boto3==1.35.85           # S3-compatible client for Oracle Object Storage

# --- security (phase 9) ---
python-magic==0.4.27
# Rate limiting is implemented in-process via a small custom dependency
# (`app/api/limiter.py`). slowapi was tried first but its decorator collided
# with FastAPI's response-model introspection on the multipart upload route.

# --- date parsing ---
python-dateutil>=2.9.0   # already a transitive dep of pandas; made explicit here

# --- OCR language auto-detect (sample PDF text layer → tess hints) ---
langdetect==1.0.9

# --- blog generation ---
google-genai==1.16.1
openai==1.59.6
anthropic==0.42.0

# --- dev ---
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
watchdog==6.0.0      # watchmedo auto-restart for Celery hot-reload in dev
